/*Jerome Hayes Jrhw4.cpp3/19/2016This program first input a file of integers,then sort and print the sorted sequence.Second, the program input a file of strings, than sort and print the sorted sequence.100% done*/#include <iostream>#include <vector>#include <fstream>#include <string>using namespace std;template<class Iterator>void adjustheap(Iterator start, unsigned int heapsize, unsigned int position){    while (position < heapsize) {        unsigned int childpos = position * 2 +1;        if (childpos < heapsize) {            if ((childpos + 1 < heapsize) and start[childpos +1] > start[childpos])                childpos++;            if(start[position] > start[childpos])                return;            else                swap (start[position], start[childpos]);        }        position = childpos;    }    }//O(log n)template<class Iterator>void makeheap(Iterator start, Iterator stop){    unsigned int heapsize = stop - start;    for(int i = (heapsize / 2) - 1; i>=0; i--)        adjustheap(start,heapsize, i);}template<class Iterator>void sortheap(Iterator start, Iterator stop){    unsigned int lastpos = stop - start -1;    while(lastpos>0) {        swap(start[0],start[lastpos]);        adjustheap(start,lastpos, 0);        lastpos--;    }}//O(nlog n)template<class Iterator>void heapsort(Iterator start, Iterator stop){    makeheap(start, stop);      //build the initial heap    sortheap(start, stop);      //convert heap into sorted collection}template<class Iterator>void sort(vector<Iterator> & c){    heapsort(c.begin(), c.end());}template<class Iterator>void print(vector<Iterator> & v)    //prints values{    for(int i=0; i<v.size(); i++)        cout << v[i] <<endl;}//O(n)int main(){    vector<int> v;    int input;    ifstream a_file;    a_file.open("numbers.txt");    if(a_file.fail())   //checks for error opening first file    {        cout << "Problem opening first file";    }    a_file >> input;    while(!a_file.eof())    {        v.push_back(input);        a_file >> input;    }    a_file.close();    sort(v);    cout << "The sorted integers are:" << endl;    print(v);    vector<string> x;    string line;    ifstream b_file;    b_file.open("words.txt");        if(b_file.fail())   //checks for error opening second file    {        cout << "Problem opening second file";    }     b_file >> line;    while(!b_file.eof())    {        x.push_back(line);        b_file >> line;    }    b_file.close();    sort(x);    print(x);}