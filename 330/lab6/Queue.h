/*Jerome Hayes JrQueue.h2/21/2016This program is a Queue as an adapter. */#ifndef QUEUE_H#define QUEUE_H#include <list>using namespace std;template <class T, template <class A, class = allocator<A> > class Container = list>class Queue{public:    bool empty() const {return container.empty();}    unsigned int size() const {return container.size();}    void push_back(const T & x) {container.push_back(x);}    void pop() {container.pop_front();}       T& back() {return container.back();}    T& front() {return container.front();}private:    Container<T> container;};#endif