/*Jerome Hayes JrStack.h2/17/16This program is a stack implemented as an adapter.*/#ifndef STACK_H#define STACK_H#include <vector>using namespace std;template <class T, template < class A,class = allocator<A> > class Container = vector>class Stack{private:    Container<T> container;public:    bool empty() const {return container.empty();}    unsigned int size() const {return container.size();}    void push(const T & x) {container.push_back(x);}    void pop() {container.pop_back();}    T & top(){return container.back();}};#endif